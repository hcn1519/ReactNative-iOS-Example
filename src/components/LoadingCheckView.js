import React, { Component } from 'react';
import { View, SafeAreaView } from 'react-native';

export default class LoadingCheckView extends Component {
  render() {
    return (
        <SafeAreaView style={{flex: 1, backgroundColor: 'blue'}}>
        </SafeAreaView>
    );
  }
}
